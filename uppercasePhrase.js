console.log("test");

//given a phrase, capitalize every word and

const test = "this is a test of the algorithm";

const capitalize = (a) => {
    const array = a.split(" ");
    console.log(array[1][0]);
    console.log(array[1][0].toUpperCase());

}

capitalize(test);