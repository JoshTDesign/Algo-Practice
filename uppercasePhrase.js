
//given a phrase, capitalize every word and

const test = "this is a test of the algorithm";

const array = test.split(" ");



const capitalize = (a) => {
    // const array = a.split(" ");
    // console.log(array[1][0]);
    // console.log(array[1][0].toUpperCase());
    // a[0] = a[0].toUpperCase();
    console.log(a[0].toUpperCase())
    a
    console.log(a)
}

const newArray = array.map(capitalize)

